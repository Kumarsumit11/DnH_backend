export enum Bucket {
  COMPANY_DOCUMENTS = 'company-documents',
  INVESTOR_DOCUMENTS = 'investor-documents',
  AVATARS = 'avatars',
  LOGOS = 'logos',
  KYC = 'kyc',
  PITCH_DECKS = 'pitch-decks'
}

export const ALL_BUCKETS = Object.values(Bucket);
